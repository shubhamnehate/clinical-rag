# Slim dependency set for the hosted Streamlit demo (app.py).
# The full clinical-rag/requirements.txt lists the production stack
# (Postgres, Redis, Elasticsearch, sentence-transformers, Anthropic SDK, ...);
# the demo runs the same pipeline in-memory with graceful fallbacks and
# talks to the Yolo-Auto LLM API over plain HTTP.
streamlit>=1.32,<2
numpy>=1.24,<3
scikit-learn>=1.3,<2
rank-bm25>=0.2.2,<1
pydantic>=2.0,<3
python-dateutil>=2.8,<3
PyYAML>=6.0,<7
structlog>=23.1,<26
